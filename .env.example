# Neon Postgres (Vercel Storage > Postgres provisions this)
DATABASE_URL=

# NextAuth
# Generate with: openssl rand -base64 32
AUTH_SECRET=
# Google OAuth (https://console.cloud.google.com/apis/credentials)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
